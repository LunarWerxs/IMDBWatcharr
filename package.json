{
  "name": "imdbwatch",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/test-parser.mjs",
    "check:web": "node scripts/test-unfollow-ui.mjs && npm --workspace web run lint",
    "//check:deep": "The everything lane, run before a release: the parser fixtures and the whole web lane (its unfollow-UI contract test, then oxlint). For the Architect's verification-coverage-reachability oracle: it walks package scripts through a literal `--cwd <dir>` edge and cannot parse `npm --workspace web run lint` in check:web below, so this lane alone still reads as web's lint being reached by no gate. The edge that counts is CI's `Lint the web app` step, which runs `npm run lint` with `working-directory: web` (checked 2026-09-16 against the oracle: the lane alone leaves the finding open, that step clears it).",
    "check:deep": "npm run check && npm run check:web",
    "test": "node --test \"test/**/*.test.mjs\" && npm run check && npm run check:web",
    "dev": "wrangler dev --remote",
    "web:dev": "npm run dev --workspace web",
    "web:build": "npm run build --workspace web",
    "build": "npm run web:build",
    "deploy": "npm run web:build && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply imdbwatcharr-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply imdbwatcharr-db --remote",
    "og": "node scripts/make-og.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "wrangler": "^4.133.0"
  }
}
