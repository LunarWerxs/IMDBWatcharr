/// <reference types="vite/client" />

// Injected by vite.config.ts `define`, from the repo root package.json
// version. Used only as an anonymous build stamp on the visit ping
// (see src/lib/analytics.ts).
declare const __APP_VERSION__: string
